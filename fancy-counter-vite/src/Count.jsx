// import { useState } from "react"; moved to Card.jsx
export default function Count({ count }) {
  //const [count, setCount] = useState(0); moved to Card.jsx
  return <p className="count">{count}</p>;
}

//export default Count;
